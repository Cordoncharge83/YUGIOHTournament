# Yu-Gi-Oh Tournament Companion

A web application designed to act as a companion and public broadcast layer for **Konami Tournament Software (KTS)** during local Yu-Gi-Oh tournaments.

Instead of replacing KTS, this project focuses on improving the player experience by providing:

- Mobile-friendly public pairings
- Public round display
- QR code sharing
- Live tournament information
- KTS CSV import workflow
- Lightweight local tournament broadcasting

---

# Project Vision

Most local communities currently rely on:

- Printed pairings
- Discord screenshots
- Google Forms
- Crowding around the organizer’s laptop

Meanwhile, KTS itself is:

- Desktop-only
- Organizer-focused
- Not designed for player mobile access

This project bridges that gap.

The organizer continues using KTS normally, then exports the current round pairings as CSV and imports them into this application.

Players can then:

- Scan a QR code
- Open the public page
- Instantly view pairings and round information from their phones

---

# Current Features

## Tournament Management

- Create tournaments
- Tournament list view
- Tournament detail page

## KTS CSV Import

- Import pairings exported from KTS
- Automatically:
  - Create players
  - Create/update rounds
  - Create/update matches
  - Set current round
- Import preview before confirmation
- Replace existing round matches safely
- BYE handling support

## Public Tournament Page

- Mobile-friendly public view
- Current round display
- Round tabs
- Public pairings
- Match result display
- QR code sharing

## Match Results

KTS-style result workflow:

- Player 1 wins
- Player 2 wins
- Draw
- Double loss
- Unreported

Designed for fast tournament-day updates.

## Sharing

- Public tournament link
- Copy link button
- QR code modal

---

# Tech Stack

## Frontend

- React
- Vite
- Tailwind CSS
- TypeScript

## Backend

- FastAPI
- SQLAlchemy
- PostgreSQL

## Database

- PostgreSQL (Docker)

---

# Architecture Philosophy

This project intentionally does **NOT** attempt to replace Konami Tournament Software.

Instead, it acts as:

- A companion platform
- A public-facing display layer
- A mobile-friendly tournament viewer

Primary workflow:

```text
KTS
→ Export current round CSV
→ Import into app
→ Public page updates
```

---

# Local Development

## Requirements

- Python 3.11+
- Node.js
- Docker Desktop

---

# Backend Setup

Start PostgreSQL:

```bash
docker compose up -d
```

Activate virtual environment:

```bash
cd backend
.venv\Scripts\activate
```

Run FastAPI:

```bash
uvicorn app.main:app --reload
```

Backend runs on:

```text
http://127.0.0.1:8000
```

Swagger docs:

```text
http://127.0.0.1:8000/docs
```

---

# Frontend Setup

```bash
cd frontend
npm install
npm run dev
```

Frontend runs on:

```text
http://127.0.0.1:5173
```

---

# CSV Import Notes

Currently supported:

- KTS exported pairing CSVs for the current round

Used columns:

- Table
- Player 1
- Player 2

Extra KTS export columns are ignored.

BYEs are handled automatically and are not added as players.

---

# Future Ideas

Potential future improvements:

- Public standings
- Standings import
- TV/display mode
- Real-time sync
- Desktop/offline version (Tauri)
- OBS overlay support
- Tournament archive/history
- Better mobile polish

---

# Status

Currently in active development as an MVP / portfolio project.

Focus is on:

- Reliability
- Tournament-day usability
- Clean mobile experience
- KTS interoperability
